migration:
  # ZAMUS_MIGRATION_DATASOURCE
  datasource: root@/dbname?parseTime=true
  # ZAMUS_MIGRATION_DIR
  dir: "migration/prod"

deploy:
  filepath: lastcommit
  folders:
    - [zamus/config, service]
    - [dql]
  steps:
    dev:
      - go test ./...
    prod:
      - go version
