migration:
  # ZAMUS_MIGRATION_DATASOURCE
  datasource: root@/dbname?parseTime=true
  # ZAMUS_MIGRATION_DIR
  dir: "migration/prod"

ci:
  filepath: lastcommit
  folders:
    - [service, replay]
    - [dql]
  steps:
    test:
      - go test ./...
    deploy:
      - go version
