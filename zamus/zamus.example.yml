deploy:
  filepath: lastcommit
  watch:
    - folders: [zamus/config, service]
      steps:
        dev:
          - go test ./...
        prod:
          - go version
    - folders: [dql]
      steps:
        dev:
          - go test ./...
        prod:
          - go version
